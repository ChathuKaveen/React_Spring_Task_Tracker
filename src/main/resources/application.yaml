spring:
  application:
    name: Task-Management
  datasource:
    url: jdbc:mysql://localhost:3306/task_management?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    show-sql: true

